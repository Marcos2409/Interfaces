{
  "name": "bicicletas",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "prestart": "stylelint \"**/*.{css,scss}\"",
    "start": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "stylelint": "^16.10.0",
    "stylelint-config-standard": "^36.0.1",
    "stylelint-use-nesting": "^6.0.0",
    "vite": "^4.4.5"
  }
}
